// Spinner.js

import React from 'react';
import './Spinner.css'; // Import or define your own styles

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
